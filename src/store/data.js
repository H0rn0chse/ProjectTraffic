import { generateId, clone } from "./shared";

const initialState = () => ({
    currentData: {},
    data: {}
});

const getters = {
};

const mutations = {
};

const actions = {
};

export default {
    namespaced: true,
    state: initialState,
    getters,
    actions,
    mutations,
    modules: {}
};
